Взять простой HTTP API (например, сервис, который складывает числа или возвращает JSON), создать нагрузку и оптимизировать его по CPU и памяти.

    pprof и net/http/pprof

    benchstat, go test -bench

    анализ trace

Результат: проект с кодом API, профилировкой с историей коммитов, README с описанием изменений.
